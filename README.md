# MySQL Student-Course Management

This Python project demonstrates how to connect to a MySQL server, create a database, define tables with relationships, and perform CRUD operations using the `mysql.connector` library.


## 🚀 Features
- Connects to a MySQL server
- Creates a database (`Rx2DB`)
- Defines two tables:
  - **Etudiants** (Students)
  - **Cours** (Courses)
- Inserts, updates, and deletes records
- Demonstrates foreign key relationships with cascading deletes
- Displays final database contents


## 🧠 Learning Objectives
This project is ideal for students learning:
- Python-MySQL integration
- SQL table creation and constraints
- CRUD (Create, Read, Update, Delete) operations
- Database automation scripts

## 🛠 Requirements
Make sure you have:
- **Python 3.8+**
- **MySQL Server** running locally
- **mysql-connector-python** library

Install the required library with:
```bash
pip install mysql-connector-python

